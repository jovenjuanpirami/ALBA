node_modules
.next
out
build
.env
.env.local
.env*.local
.vercel
*.tsbuildinfo
next-env.d.ts
.DS_Store
Thumbs.db

# Basura que dejan las herramientas de Windows/Cygwin al caerse.
*.stackdump

# Renders originales sin comprimir (~43 MB). Viven en OneDrive, no en git:
# el historial de git no los suelta nunca y harían lento cada clone.
# Lo que el sitio usa de verdad es media/, ya optimizado y sí versionado.
Assets/
